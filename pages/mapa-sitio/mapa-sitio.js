document.addEventListener('DOMContentLoaded',()=>{const cats=[...new Set(SIDI_DATA.reports.map(r=>r.category))].slice(0,12);document.getElementById('mapBoard').innerHTML=cats.map(c=>`<article class="map-node"><h2>${SIDI.icon('folder','sidi-icon--folder')} ${c}</h2>${SIDI_DATA.reports.filter(r=>r.category===c).slice(0,4).map(r=>`<a href="visor-bi.html?reporte=${r.id}">${r.title}</a>`).join('')}</article>`).join('')});